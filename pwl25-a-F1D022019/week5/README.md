## Masuk ke Direktori week5

```bash
cd week5
```

## Install Dependencies

```bash
npm install --save-dev html-webpack-plugin copy-webpack-plugin favicons-webpack-plugin clean-webpack-plugin webpack-dev-server webpack-cli style-loader css-loader file-loader jsdom webpack
```

## Jalankan Dev Server

```bash
./node_modules/.bin/webpack-dev-server
```

## Akses di Browser

Akses di browser di alamat `http://localhost:8080` (atau port lain jika 8080 sudah terpakai).